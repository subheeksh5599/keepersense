# Frontend config — copy to .env.local and adjust as needed.
# Nothing here is required; all values have safe defaults.

# Chain used for workflow deploy/execute + explorer links (default: sepolia)
VITE_CHAIN=sepolia
